import React, { useState } from "react";

export default function CreateScore(rightEmmet, typedText) {
  //condition ? exprIfTrue : exprIfFalse
  let score = 0;
  if (typedText == rightEmmet) {
  //>>  setScoreState(scoreState + 10);
  }

  //typedText == rightEmmet ? setScoreState(scoreState + 10) : score;

  //>> return scoreState;
}
